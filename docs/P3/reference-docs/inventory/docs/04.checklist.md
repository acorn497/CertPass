# 구현 체크리스트

> 구현 진행 시 완료된 항목에 `[x]` 표시를 한다.

## Phase 0. Android 플랫폼 설정

- [x] `AndroidManifest.xml`에 카메라 권한 추가 (`CAMERA`)
- [x] `build.gradle.kts` minSdk 21 이상 확인
- [x] 앱 라벨 한글화 (`android:label` → "소모품 재고")

## Phase 1. 프로젝트 기반

- [x] 의존성 설치 — 상태관리: `flutter_riverpod`, `riverpod_annotation`
- [x] 의존성 설치 — DB: `drift`, `sqlite3_flutter_libs`
- [x] 의존성 설치 — 라우팅: `go_router`
- [x] 의존성 설치 — 모델: `freezed_annotation`, `json_annotation`
- [x] dev 의존성 설치 — `drift_dev`, `build_runner`, `freezed`, `json_serializable`, `riverpod_generator`
- [x] 프로젝트 디렉토리 구조 생성 (`lib/models`, `lib/db`, `lib/screens`, `lib/widgets`, `lib/providers`, `lib/router`, `lib/theme` 등)
- [x] 앱 테마 설정 (Material Design 3, UI 목업 기준 색상·타이포)
- [x] 라우팅/네비게이션 설정 (`go_router` — 홈, 스캔, 등록, 상세, 수정, 검색)
- [x] `main.dart` 리팩터링 (`ProviderScope` 래핑, `app.dart` 분리)

## Phase 2. 데이터베이스

- [x] drift 테이블 정의 (`products`, `inventory_items`, `storage_locations`, `app_settings`)
- [x] 모델 클래스 정의 — freezed (`Product`, `InventoryItem`, `StorageLocation`, `AppSetting`)
- [x] 데이터베이스 클래스 생성 (`@DriftDatabase`, FK 활성화, CASCADE, 인덱스)
- [x] Seed 데이터 삽입 로직 (`storage_locations` 28건, `app_settings` 1건)
- [x] DAO 구현 — Products (CRUD, 바코드 검색, 이름 검색, 총 수량 집계 조인)
- [x] DAO 구현 — InventoryItems (CRUD, 상품별 조회, 임박/만료 조회, 수량 증감)
- [x] DAO 구현 — StorageLocations (전체 목록, 추가)
- [x] DAO 구현 — AppSettings (읽기, 쓰기/upsert)
- [x] drift 코드 생성 (`dart run build_runner build`)
- [x] DB 단위 테스트

## Phase 3. 상태관리 및 비즈니스 로직

- [x] database_provider (AppDatabase 싱글톤 제공)
- [x] Product 상태관리 (목록 조회, 검색, 총 수량 집계)
- [x] InventoryItem 상태관리 (상품별 재고 목록, 수량 증감)
- [x] 사용기한 임박/만료 필터 로직 (`app_settings.expiration_alert_days` 기준)
- [x] StorageLocation 상태관리 (목록 조회)
- [x] AppSetting 상태관리 (설정값 읽기/쓰기)

## Phase 4. 공통 위젯

- [x] 수량 조절 위젯 (QuantityStepper: +/- 버튼 + 직접 입력, 0 이하 방지)
- [x] 사용기한 입력 위젯 (DatePicker + 무기한 토글)
- [x] 저장 위치 선택 위젯 (드롭다운, storage_locations 목록 연동)
- [x] 삭제 확인 다이얼로그 (상품명 + 재고 건수 경고)
- [x] 검색바 위젯 (텍스트 입력 + 바코드 스캔 버튼)

## Phase 5. 홈 화면

- [x] 홈 화면 기본 레이아웃 (AppBar, FAB, 검색바)
- [x] 상품 목록 표시 (이름, 총 수량, 저장 위치, 사용기한 상태)
- [x] 사용기한 경고 섹션 (만료 + 임박 아이템, 상단 배치, 빨강/주황 배지)
- [x] 빈 상태 화면 (상품 0건, 안내 메시지 + FAB 유도)
- [x] 등록 방법 선택 Bottom Sheet (바코드 스캔 / 직접 입력)

## Phase 6. 바코드 스캔

- [x] 바코드 스캔 라이브러리 설치 (`mobile_scanner`)
- [x] Android 카메라 권한 런타임 요청 처리 (F-SCAN-03)
- [x] 바코드 스캔 화면 (MobileScanner 위젯, 카메라 뷰파인더, 스캔 가이드 오버레이)
- [x] 권한 거부 시 안내 + 설정 이동 버튼
- [x] 스캔 결과 반환 (등록용 / 검색용 mode 구분)

## Phase 7. 상품 등록

- [x] 새 상품 등록 화면 — 바코드 스캔 후 (상품 정보 + 첫 재고 입력)
- [x] 새 상품 등록 화면 — 수동 입력 (바코드 없는 상품)
- [x] 기존 상품 발견 시 재고 추가 화면 (F-ITEM-05, 바코드 중복)
- [x] 폼 유효성 검증 (상품명 필수, 수량 >= 0)
- [x] 저장 로직 (Product + InventoryItem 트랜잭션 동시 생성)

## Phase 8. 상품 상세 및 재고 관리

- [x] 상품 상세 화면 (상품 정보 + 재고 항목 목록)
- [x] 재고 항목별 수량 빠른 조절 (+/- 버튼)
- [x] 재고 항목 추가 화면 (기존 상품에 새 재고)
- [x] 재고 항목 수정 화면 (수량, 사용기한, 저장 위치, 메모)
- [x] 재고 항목 삭제 (개별 삭제)
- [x] 상품 수정 (이름, 바코드)
- [x] 상품 삭제 (CASCADE 경고 다이얼로그 포함)

## Phase 9. 검색

- [x] 이름 텍스트 검색 (부분 일치, 검색 결과 목록)
- [x] 바코드 스캔 검색 (스캔 → 상품 상세로 이동)
- [x] 검색 키워드 하이라이트 표시

## 버그 수정

- [x] BUG-001: 재고 수정 화면에서 Back 시 상세 화면 Back 버튼 소실 및 앱 종료 — `context.go()` → `context.push()` 변경 (상세→재고 추가/수정 네비게이션)

## 매 Phase 공통

- [x] `flutter analyze` 경고/에러 0건
- [x] `flutter test` 전체 통과
